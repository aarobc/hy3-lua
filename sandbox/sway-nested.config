# Minimal nested sway config for hy3-lua comparison testing.
# Launch with: sway -c ~/code/hy3-lua/sandbox/sway-nested.config
# See ../CLAUDE.md for the full workflow.

set $mod Mod4

bindsym $mod+h focus left
bindsym $mod+l focus right
bindsym $mod+k focus up
bindsym $mod+j focus down

bindsym $mod+Shift+h move left
bindsym $mod+Shift+l move right
bindsym $mod+Shift+k move up
bindsym $mod+Shift+j move down

bindsym $mod+Return exec foot
bindsym $mod+q kill

# split orientation (no sway defaults; added for parity with hypr config)
bindsym $mod+v splitv
bindsym $mod+s splith
bindsym $mod+t splitt

default_border none
gaps inner 4
